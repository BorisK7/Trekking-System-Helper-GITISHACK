# 🎭 Система Анализа Театральных Постановок

Интеллектуальная система компьютерного зрения для автоматического анализа театральных постановок с использованием YOLOv8 и глубокого обучения.

## 📋 Описание

Система предназначена для автоматического анализа видеозаписей театральных представлений, выполняя:
- 🎯 **Детекция объектов** - распознавание актёров, мебели, реквизита
- 📍 **Трекинг перемещений** - отслеживание траекторий движения актёров по сцене
- 🗺️ **Карта проекций** - создание топ-вью проекции сцены с траекториями
- 📊 **Аналитика активности** - тепловые карты, зоны активности, статистика
- 💾 **Экспорт данных** - сохранение результатов в JSON/CSV для дальнейшего анализа

### Бизнес-применение

- **Режиссёры**: Анализ мизансцен и движения актёров на сцене
- **Хореографы**: Визуализация траекторий движения для постановки танцев
- **Театральные критики**: Объективные данные о композиции и динамике спектакля
- **Архивы**: Автоматическая каталогизация и метаданные постановок
- **Обучение**: Изучение пространственной организации классических постановок

## ✨ Ключевые возможности

### 1. Детекция объектов (80 классов COCO)
```bash
Актёры: person
Мебель: chair, couch, bed, dining table
Реквизит: potted plant, vase, bottle, cup, book
```

### 2. Интеллектуальный трекинг
- Уникальный ID для каждого объекта на протяжении всего видео
- Алгоритм Hungarian для точного сопоставления
- Сохранение идентичности при временных окклюзиях

### 3. Карта проекций сцены
- Отдельное видео с видом сверху (top-down view)
- Визуализация траекторий движения актёров
- Направления движения (стрелки)
- Текущие позиции всех объектов

### 4. Аналитика
- **Heat Map** - зоны максимальной активности на сцене
- **Траектории** - полные пути перемещения с градиентной окраской
- **Статистика** - количество объектов, распределение по классам
- **Временные метрики** - время нахождения в кадре

## 🚀 Быстрый старт

### Системные требования

**Минимальные:**
- Python 3.8+
- 8 GB RAM
- CPU: Intel Core i5 / AMD Ryzen 5

**Рекомендуемые (для GPU):**
- NVIDIA GPU с CUDA (GTX 1060 6GB+)
- 16 GB RAM
- CUDA 11.8 или 12.x

### Установка

#### 1. Клонирование репозитория
```bash
git clone https://github.com/BorisK7/AudioSystems_GITISHACK.git
cd AudioSystems_GITISHACK/people_detection
```

#### 2. Создание виртуального окружения
```powershell
# Windows
python -m venv yolo8_env
.\yolo8_env\Scripts\Activate.ps1

# Linux/macOS
python3 -m venv yolo8_env
source yolo8_env/bin/activate
```

#### 3. Установка зависимостей

**Для CPU:**
```bash
pip install -r requirements.txt
```

**Для GPU (NVIDIA):**
```bash
# Сначала PyTorch с CUDA
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# Затем остальные зависимости
pip install ultralytics opencv-python scipy numpy matplotlib
```

#### 4. Проверка GPU (опционально)
```bash
python -c "import torch; print('CUDA:', torch.cuda.is_available(), '| GPU:', torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'N/A')"
```

## 📖 Использование

### Базовый запуск

```bash
# Просмотр в реальном времени (без сохранения)
python main.py --source input/video.mp4

# С сохранением результата
python main.py --source input/video.mp4 --output output/result.mp4
```

### Полный функционал

```bash
python main.py \
  --source input/performance.mp4 \
  --output output/analyzed.mp4 \
  --model yolov8m \
  --classes "person,chair,couch,bed,dining table,potted plant,vase" \
  --conf 0.5 \
  --show-trajectories \
  --show-heatmap \
  --show-stats-panel \
  --export-data \
  --export-dir exports
```

### Параметры командной строки

| Параметр | Описание | Значение по умолчанию |
|----------|----------|----------------------|
| `--source` | Путь к видеофайлу | **Обязательно** |
| `--output` | Путь для сохранения результата | Не сохраняется |
| `--model` | Модель YOLOv8 (n/s/m/l/x) | `yolov8m` |
| `--classes` | Классы для детекции (через запятую) | Все классы |
| `--conf` | Порог уверенности (0.0-1.0) | `0.5` |
| `--iou` | Порог IoU для NMS | `0.45` |
| `--show-trajectories` | Показать траектории движения | Выкл |
| `--show-heatmap` | Показать тепловую карту | Выкл |
| `--show-stats-panel` | Панель статистики | Выкл |
| `--export-data` | Экспорт данных в JSON/CSV | Выкл |
| `--export-dir` | Папка для экспорта | `exports` |
| `--no-display` | Без отображения (только сохранение) | Выкл |
| `--no-tracking` | Отключить трекинг | Вкл |

### Модели YOLOv8

| Модель | Размер | Скорость | Точность | Рекомендация |
|--------|--------|----------|----------|--------------|
| `yolov8n` | 6 MB | ⚡⚡⚡⚡⚡ | ⭐⭐ | CPU, быстрый просмотр |
| `yolov8s` | 22 MB | ⚡⚡⚡⚡ | ⭐⭐⭐ | CPU, баланс |
| `yolov8m` | 52 MB | ⚡⚡⚡ | ⭐⭐⭐⭐ | **Рекомендуется** |
| `yolov8l` | 87 MB | ⚡⚡ | ⭐⭐⭐⭐⭐ | GPU, высокая точность |
| `yolov8x` | 136 MB | ⚡ | ⭐⭐⭐⭐⭐ | GPU, максимальная точность |

## 📁 Структура проекта

```
people_detection/
├── main.py                      # Главный файл запуска
├── requirements.txt             # Зависимости Python
├── README.md                    # Документация
├── .gitignore                   # Исключения Git
│
├── input/                       # Входные видео
│   ├── example.mp4             # Пример видео
│   └── .gitkeep
│
├── output/                      # Результаты обработки
│   ├── result.mp4              # Основное видео с аналитикой
│   ├── floor_projection.mp4    # Карта проекций (top-down view)
│   └── .gitkeep
│
├── exports/                     # Экспортированные данные
│   ├── data_YYYYMMDD_HHMMSS.json
│   ├── data_YYYYMMDD_HHMMSS.csv
│   └── .gitkeep
│
└── utils/                       # Модули системы
    ├── __init__.py             
    ├── config.py               # Конфигурация и константы
    ├── video_processor_advanced.py  # Основной процессор видео
    ├── tracker.py              # Трекинг объектов (Hungarian)
    ├── analytics.py            # Аналитика (heat maps, зоны)
    ├── visualizer.py           # Визуализация траекторий
    ├── distance.py             # Расчёт дистанций
    └── exporter.py             # Экспорт данных
```

## 🎬 Выходные файлы

### 1. Основное видео (`output/result.mp4`)
- Bounding boxes вокруг объектов
- Tracking ID и класс объекта
- Траектории движения с градиентом
- Тепловая карта активности
- Панель статистики в реальном времени

### 2. Карта проекций (`output/floor_projection.mp4`)
- Вид сверху (800x600 пикселей)
- Координатная сетка
- Траектории движения всех объектов
- Текущие позиции с ID
- Направления движения (стрелки)

### 3. Данные аналитики (`exports/data_*.json`)
```json
{
  "metadata": {
    "timestamp": "2025-11-23T02:37:10",
    "video_source": "input/performance.mp4",
    "total_frames": 514,
    "fps": 30.0
  },
  "objects": [
    {
      "track_id": 1,
      "class": "person",
      "frames_present": 245,
      "trajectory": [
        {"frame": 0, "x": 960, "y": 540, "confidence": 0.87},
        {"frame": 1, "x": 965, "y": 538, "confidence": 0.89}
      ]
    }
  ],
  "statistics": {
    "total_objects": 2286,
    "class_distribution": {
      "chair": 1567,
      "person": 890
    },
    "tracks_created": 9
  }
}
```

## 🔧 Настройка для театра

### Выбор классов для театральной постановки

```bash
# Только актёры
--classes "person"

# Актёры и основная мебель
--classes "person,chair,couch,bed"

# Полная сцена с реквизитом
--classes "person,chair,couch,bed,dining table,potted plant,vase,bottle,cup,book,clock,scissors,teddy bear"
```

### Оптимизация производительности

**Для записи спектаклей (высокое качество):**
```bash
--model yolov8l --conf 0.6
```

**Для репетиций (быстрая обработка):**
```bash
--model yolov8s --conf 0.4
```

**Для архивных записей (баланс):**
```bash
--model yolov8m --conf 0.5
```

## 🎨 Визуализация

### Траектории движения
- **Градиентная окраска**: от красного (начало) к зелёному (конец)
- **Толщина линии**: 4 пикселя для чёткости
- **Стрелки направления**: показывают вектор движения

### Тепловая карта
- **Красные зоны**: максимальная активность актёров
- **Синие зоны**: минимальная активность
- **Прозрачность**: 40% для наложения на видео

### Панель статистики
Отображает в реальном времени:
- Текущий кадр / Всего кадров
- FPS обработки
- Количество объектов в кадре
- Распределение по классам

## 🐛 Решение проблем

### GPU не используется

**Проблема:** `Устройство: CPU (CUDA не доступна)`

**Решение:**
```bash
pip uninstall torch torchvision -y
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
python -c "import torch; print('CUDA:', torch.cuda.is_available())"
```

### Низкая производительность

**CPU:** 4-5 FPS - нормально  
**GPU:** 60-80 FPS - ожидается

**Ускорение:**
1. Уменьшите модель: `--model yolov8n`
2. Увеличьте порог: `--conf 0.6`
3. Отключите визуализацию: `--no-display`

### Слишком много ложных срабатываний

**Решение:**
```bash
# Увеличьте порог уверенности
--conf 0.7

# Укажите конкретные классы
--classes "person,chair"
```

### Потеря трекинга объектов

**Причины:**
- Быстрые движения актёров
- Перекрытия (окклюзии)
- Низкая уверенность детекций

**Решение:**
```bash
# Снизьте порог для большего количества детекций
--conf 0.4

# Используйте более точную модель
--model yolov8l
```

## 📊 Производительность

### Тесты на разном железе

| Конфигурация | FPS | Время обработки (10 мин видео) |
|--------------|-----|-------------------------------|
| Intel i5-9400F (CPU) | 4-5 | ~40 минут |
| NVIDIA GTX 1060 6GB | 45-55 | ~4 минуты |
| NVIDIA RTX 3060 12GB | 70-80 | ~2.5 минуты |
| NVIDIA RTX 4090 24GB | 120-150 | ~1.5 минуты |

### Потребление ресурсов

| Ресурс | YOLOv8n | YOLOv8m | YOLOv8x |
|--------|---------|---------|---------|
| VRAM (GPU) | 1-2 GB | 3-4 GB | 8-10 GB |
| RAM | 2 GB | 4 GB | 6 GB |
| Диск (модель) | 6 MB | 52 MB | 136 MB |

## 📝 Примеры использования

### Анализ репетиции
```bash
python main.py \
  --source rehearsal_2025-11-23.mp4 \
  --output analyzed/rehearsal_analyzed.mp4 \
  --classes "person" \
  --show-trajectories \
  --export-data
```

### Создание архивной записи
```bash
python main.py \
  --source archive/hamlet_1985.mp4 \
  --output archive/hamlet_1985_analyzed.mp4 \
  --model yolov8m \
  --classes "person,chair,bed,dining table" \
  --show-heatmap \
  --show-stats-panel \
  --no-display \
  --export-data
```

### Быстрый просмотр мизансцен
```bash
python main.py \
  --source mise_en_scene.mp4 \
  --model yolov8s \
  --classes "person" \
  --show-trajectories
```

## 🔄 История обновлений

### v1.0.0 (23.11.2025)
- ✅ Базовая детекция объектов YOLOv8 (80 классов COCO)
- ✅ Трекинг с Hungarian algorithm
- ✅ Карта проекций сцены (floor projection)
- ✅ Траектории движения с градиентами
- ✅ Тепловые карты активности
- ✅ Экспорт в JSON/CSV
- ✅ Поддержка CUDA/GPU
- ✅ Панель статистики в реальном времени

## 🤝 Контакты и поддержка

- **GitHub**: [BorisK7/AudioSystems_GITISHACK](https://github.com/BorisK7/AudioSystems_GITISHACK)
- **Issues**: [Сообщить о проблеме](https://github.com/BorisK7/AudioSystems_GITISHACK/issues)

## 📄 Лицензия

Этот проект использует следующие компоненты:
- **YOLOv8**: Ultralytics (AGPL-3.0)
- **PyTorch**: BSD-style license
- **OpenCV**: Apache 2.0

## 🙏 Благодарности

- Ultralytics за YOLOv8
- PyTorch community
- OpenCV developers

---

**Made with ❤️ for Theater Analytics**

### Пример 4: Быстрая обработка большого файла

```bash
python main.py --model yolov8n --source input/long_video.mp4 --output output/fast_result.mp4 --no-display
```

## 📊 Производительность на RTX 3060

Примерная производительность для видео 1080p:

| Модель | FPS (RTX 3060) |
|--------|----------------|
| yolov8n | ~140 FPS |
| yolov8s | ~110 FPS |
| yolov8m | ~70 FPS |
| yolov8l | ~45 FPS |
| yolov8x | ~30 FPS |

*Значения могут варьироваться в зависимости от сложности сцены

## 🐛 Решение проблем

### CUDA не обнаружена

```bash
# Проверка версии PyTorch
python -c "import torch; print(torch.__version__)"

# Переустановка PyTorch с CUDA 11.8
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
```

### Низкий FPS

1. Используйте более легкую модель (yolov8n или yolov8s)
2. Уменьшите разрешение входного видео
3. Увеличьте `--conf` для фильтрации слабых детекций

### Файл не найден

```bash
# Создайте необходимые директории
mkdir input output
```

## 📝 Логи и отладка

Программа выводит подробные логи:

```
2025-11-23 10:30:15 - INFO - Загрузка модели yolov8m...
2025-11-23 10:30:18 - INFO - Устройство: GPU: NVIDIA GeForce RTX 3060 (12.0 GB)
2025-11-23 10:30:18 - INFO - Разрешение: 1920x1080, FPS: 30
2025-11-23 10:30:18 - INFO - Результат будет сохранен в: output/result.mp4
2025-11-23 10:30:18 - INFO - Начало обработки видео...
```

## 🎨 Визуализация

Каждый кадр содержит:
- ✅ Зеленые bounding boxes вокруг людей
- ✅ Метки с уверенностью детекции
- ✅ FPS в реальном времени
- ✅ Количество обнаруженных людей
- ✅ Номер текущего кадра

## 📈 Статистика после обработки

После завершения выводится:
- Общее количество обработанных кадров
- Всего обнаружено людей
- Среднее количество людей на кадр

## 🔧 Расширенная настройка

### Изменение цвета боксов

Отредактируйте `utils/config.py`:

```python
BOX_COLOR = (0, 0, 255)  # Красный (BGR формат)
```

### Изменение толщины линий

```python
BOX_THICKNESS = 3  # Более толстые боксы
```

## 📄 Лицензия

MIT License

## 👨‍💻 Автор

YOLOv8 People Detection System для NVIDIA RTX 3060

## 🤝 Поддержка

При возникновении проблем создайте Issue в репозитории проекта.

---

**Примечание**: Убедитесь, что у вас установлены драйверы NVIDIA и CUDA Toolkit для работы с GPU.
