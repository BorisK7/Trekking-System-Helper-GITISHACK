# 🎯 Руководство по режиму динамических объектов

## Описание

Новый режим **"Только динамические объекты"** позволяет детектировать и отслеживать **только движущиеся объекты**, автоматически фильтруя статичные элементы сцены (мебель, стены, неподвижные предметы).

### Возможности

✅ **Автоматическая фильтрация** - показываются только объекты с движением  
✅ **Классификация движения** - определение типа движения (ходьба, бег, транспорт и т.д.)  
✅ **Метрики движения** - скорость, направление, ускорение  
✅ **Адаптивные пороги** - настройка чувствительности детекции движения

---

## 🚀 Быстрый старт

### Базовое использование
```bash
python main.py --source input/video.mp4 --dynamic-only
```

### С настройкой порога движения
```bash
# Более чувствительный (детектирует медленное движение)
python main.py --source input/video.mp4 --dynamic-only --motion-threshold 3.0

# Менее чувствительный (только быстрое движение)
python main.py --source input/video.mp4 --dynamic-only --motion-threshold 10.0
```

### С адаптивным трекингом
```bash
python main.py --source input/video.mp4 --dynamic-only --adaptive-tracking
```

### Полная конфигурация
```bash
python main.py \
  --source input/video.mp4 \
  --dynamic-only \
  --motion-threshold 5.0 \
  --adaptive-tracking \
  --show-trajectories \
  --show-heatmap \
  --export-data \
  --classes person
```

---

## 📊 Классификация типов движения

### Для людей (person)
| Тип движения | Скорость (px/s) | Описание |
|--------------|-----------------|----------|
| `slow_walk` | < 20 | Медленная ходьба |
| `walk` | 20-50 | Обычная ходьба |
| `fast_walk` | 50-100 | Быстрая ходьба |
| `run` | 100-200 | Бег |
| `sprint` | > 200 | Спринт |

### Для транспорта (car, bicycle, motorcycle, etc.)
| Тип движения | Скорость (px/s) | Описание |
|--------------|-----------------|----------|
| `vehicle_slow` | < 50 | Медленное движение |
| `vehicle_medium` | 50-150 | Среднее движение |
| `vehicle_fast` | > 150 | Быстрое движение |

### Для животных (cat, dog, bird, etc.)
| Тип движения | Описание |
|--------------|----------|
| `animal_slow` | Медленное движение |
| `animal_fast` | Быстрое движение |
| `animal_erratic` | Хаотичное движение (высокое ускорение) |

### Для других объектов
| Тип движения | Описание |
|--------------|----------|
| `slow_motion` | Медленное движение |
| `fast_motion` | Быстрое движение |
| `erratic` | Хаотичное движение |

---

## ⚙️ Параметры настройки

### `--motion-threshold` (по умолчанию: 5.0)
Минимальное движение в пикселях для считывания объекта динамическим.

**Рекомендации:**
- **3.0-5.0** - для детекции медленного движения (люди, пешеходы)
- **5.0-10.0** - стандартная чувствительность
- **10.0-20.0** - только быстрое движение (транспорт, бег)

### Внутренние параметры (в коде)
```python
DynamicObjectFilter(
    movement_threshold=5.0,    # Порог движения (пикселей)
    history_length=10,         # Длина истории для анализа (кадров)
    min_frames_to_confirm=3    # Минимум кадров для подтверждения
)
```

---

## 📈 Метрики движения

Для каждого динамического объекта вычисляются:

### 1. **Скорость (Speed)**
- Измеряется в пикселях/секунду
- Рассчитывается как общее пройденное расстояние / время

### 2. **Направление (Direction)**
- Угол в градусах (-180° до 180°)
- 0° = движение вправо
- 90° = движение вниз
- -90° = движение вверх

### 3. **Ускорение (Acceleration)**
- Изменение скорости во времени
- Положительное = ускорение
- Отрицательное = замедление

### 4. **Длина траектории (Trajectory Length)**
- Общее пройденное расстояние в пикселях

---

## 🎨 Визуализация

### На экране отображается:
```
┌─────────────────────────────┐
│ MOTION: Walk                │  ← Тип движения
│ Speed: 45.3px/s             │  ← Скорость
│ ID:1                        │  ← ID трека
│ Person                      │  ← Класс объекта
└─────────────────────────────┘
```

### Цвета рамок (при адаптивном трекинге):
- 🟢 **Зеленый** - DYNAMIC (активное движение)
- 🟡 **Желтый** - MOVABLE (может двигаться)
- 🔵 **Синий** - STATIC (статичный)

---

## 💡 Примеры использования

### 1. Мониторинг пешеходов
```bash
python main.py \
  --source input/street.mp4 \
  --dynamic-only \
  --classes person \
  --motion-threshold 3.0 \
  --show-trajectories
```

### 2. Детекция транспорта
```bash
python main.py \
  --source input/road.mp4 \
  --dynamic-only \
  --classes "car,truck,bus,motorcycle" \
  --motion-threshold 10.0
```

### 3. Анализ активности в помещении
```bash
python main.py \
  --source input/office.mp4 \
  --dynamic-only \
  --adaptive-tracking \
  --show-heatmap \
  --export-data
```

### 4. Детекция животных
```bash
python main.py \
  --source input/park.mp4 \
  --dynamic-only \
  --classes "cat,dog,bird" \
  --motion-threshold 5.0
```

---

## 🔍 Как это работает

### Алгоритм фильтрации

1. **Сбор истории позиций**
   - Для каждого трека сохраняется история центроидов за последние N кадров

2. **Расчет движения**
   - Вычисляется общее пройденное расстояние
   - Сравнивается с порогом `motion_threshold`

3. **Классификация**
   - На основе скорости и ускорения определяется тип движения
   - Учитывается класс объекта (person, car, etc.)

4. **Фильтрация**
   - Объекты с движением < порога отбрасываются
   - Только динамические объекты передаются на визуализацию

### Псевдокод
```python
for each track:
    history = get_position_history(track, last_10_frames)
    total_distance = calculate_distance(history)
    
    if total_distance < motion_threshold:
        filter_out(track)  # Статичный объект
    else:
        speed = total_distance / time_span
        motion_type = classify_motion(speed, acceleration, class)
        display(track, motion_type, speed)
```

---

## 📊 Экспорт данных

При использовании `--export-data` в JSON/CSV добавляются поля:

```json
{
  "track_id": 1,
  "class": "person",
  "motion_classification": {
    "motion_type": "walk",
    "speed_px_s": 45.3,
    "direction": 90.5,
    "acceleration": 2.1,
    "trajectory_length": 450.2
  }
}
```

---

## ⚠️ Ограничения

1. **Требуется трекинг** - режим работает только с включенным трекингом
2. **Задержка классификации** - нужно 3+ кадров для подтверждения движения
3. **Зависимость от FPS** - пороги скорости зависят от частоты кадров видео
4. **Разрешение видео** - пороги в пикселях зависят от разрешения

---

## 🛠️ Настройка под свою задачу

### Изменение порогов в коде

Отредактируйте `people_detection/utils/dynamic_filter.py`:

```python
def _classify_motion_type(self, speed, acceleration, class_id, distance):
    if class_name == 'person':
        if speed < 20:        # ← Измените пороги
            return 'slow_walk'
        elif speed < 50:
            return 'walk'
        # ...
```

### Добавление новых типов движения

```python
# В методе _classify_motion_type
elif class_name == 'my_custom_object':
    if speed < 30:
        return 'custom_slow'
    else:
        return 'custom_fast'
```

---

## 🆚 Сравнение режимов

| Параметр | Обычный режим | Режим dynamic-only |
|----------|---------------|-------------------|
| Детекция статичных объектов | ✅ Да | ❌ Нет |
| Детекция движущихся объектов | ✅ Да | ✅ Да |
| Классификация движения | ❌ Нет | ✅ Да |
| Метрики скорости | ⚠️ Базовые | ✅ Расширенные |
| Производительность | 🟢 Стандартная | 🟡 Чуть ниже (доп. анализ) |

---

## 📚 Связанные файлы

- `utils/dynamic_filter.py` - Основная логика фильтрации
- `utils/visualizer.py` - Визуализация классификации
- `main.py` - Параметры командной строки
- `COMMANDS_CHEATSHEET.md` - Шпаргалка команд

---

**Дата создания:** 23 ноября 2025  
**Версия:** 1.0

