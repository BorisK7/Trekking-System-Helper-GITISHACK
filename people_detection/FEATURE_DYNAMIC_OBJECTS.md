# ✨ Новая функция: Режим динамических объектов

## Что добавлено

### 1. Новый модуль `utils/dynamic_filter.py`
Интеллектуальный фильтр для выделения только движущихся объектов с классификацией типа движения.

**Основные возможности:**
- Автоматическая фильтрация статичных объектов
- Классификация движения (ходьба, бег, транспорт и т.д.)
- Расчет метрик: скорость, направление, ускорение
- Адаптивные пороги для разных типов объектов

### 2. Новые параметры командной строки

#### `--dynamic-only`
Включает режим "только динамические объекты"

```bash
python main.py --source input/video.mp4 --dynamic-only
```

#### `--motion-threshold FLOAT`
Настройка порога движения (по умолчанию: 5.0 пикселей)

```bash
python main.py --source input/video.mp4 --dynamic-only --motion-threshold 3.0
```

### 3. Расширенная визуализация
Добавлено отображение:
- Типа движения (Walk, Run, Sprint, etc.)
- Скорости в пикселях/секунду
- Направления движения

### 4. Обновленный экспорт данных
В JSON/CSV добавлены поля классификации движения:
- `motion_type` - тип движения
- `speed_px_s` - скорость
- `direction` - направление (градусы)
- `acceleration` - ускорение
- `trajectory_length` - длина траектории

---

## Технические детали

### Архитектура

```
VideoProcessor
    ↓
ObjectTracker (трекинг объектов)
    ↓
DynamicObjectFilter (фильтрация + классификация)
    ↓
Визуализация с метриками движения
```

### Алгоритм классификации

1. **Сбор истории** - последние 10 кадров позиций
2. **Расчет движения** - общее пройденное расстояние
3. **Фильтрация** - сравнение с порогом
4. **Классификация** - определение типа по скорости и ускорению

### Классы движения

#### Для людей (person)
- `slow_walk` - < 20 px/s
- `walk` - 20-50 px/s
- `fast_walk` - 50-100 px/s
- `run` - 100-200 px/s
- `sprint` - > 200 px/s

#### Для транспорта (car, bicycle, etc.)
- `vehicle_slow` - < 50 px/s
- `vehicle_medium` - 50-150 px/s
- `vehicle_fast` - > 150 px/s

#### Для животных (cat, dog, bird)
- `animal_slow` - медленное движение
- `animal_fast` - быстрое движение
- `animal_erratic` - хаотичное (высокое ускорение)

---

## Примеры использования

### 1. Базовое использование
```bash
python main.py --source input/video.mp4 --dynamic-only
```

### 2. С настройкой чувствительности
```bash
# Детекция медленного движения
python main.py --source input/video.mp4 --dynamic-only --motion-threshold 3.0

# Только быстрое движение
python main.py --source input/video.mp4 --dynamic-only --motion-threshold 10.0
```

### 3. С адаптивным трекингом
```bash
python main.py --source input/video.mp4 --dynamic-only --adaptive-tracking \
  --show-trajectories --show-heatmap
```

### 4. С экспортом данных
```bash
python main.py --source input/video.mp4 --dynamic-only --export-data \
  --classes person
```

---

## Сценарии применения

### 🚶 Мониторинг пешеходов
```bash
python main.py --source street.mp4 --dynamic-only --classes person \
  --motion-threshold 3.0 --show-trajectories
```
**Результат:** Детекция только идущих людей, фильтрация стоящих

### 🚗 Детекция транспорта
```bash
python main.py --source road.mp4 --dynamic-only \
  --classes "car,truck,bus,motorcycle" --motion-threshold 10.0
```
**Результат:** Детекция только движущихся машин, игнорирование припаркованных

### 🏢 Анализ активности в помещении
```bash
python main.py --source office.mp4 --dynamic-only --adaptive-tracking \
  --show-heatmap --export-data
```
**Результат:** Карта активности с классификацией типов движения

### 🐕 Детекция животных
```bash
python main.py --source park.mp4 --dynamic-only \
  --classes "cat,dog,bird" --motion-threshold 5.0
```
**Результат:** Детекция только движущихся животных

---

## Преимущества

✅ **Снижение шума** - фильтрация статичных объектов  
✅ **Фокус на событиях** - только важные движущиеся объекты  
✅ **Классификация** - понимание типа движения  
✅ **Метрики** - количественные данные о движении  
✅ **Гибкость** - настройка порогов под задачу  

---

## Ограничения

⚠️ **Требуется трекинг** - режим работает только с включенным трекингом  
⚠️ **Задержка** - нужно 3+ кадров для подтверждения движения  
⚠️ **Зависимость от FPS** - пороги скорости зависят от частоты кадров  
⚠️ **Разрешение** - пороги в пикселях зависят от разрешения видео  

---

## Файлы изменений

### Новые файлы
- `utils/dynamic_filter.py` - основная логика
- `DYNAMIC_MODE_GUIDE.md` - подробное руководство
- `DYNAMIC_QUICK_START.txt` - быстрый старт
- `FEATURE_DYNAMIC_OBJECTS.md` - этот файл

### Измененные файлы
- `main.py` - добавлены параметры `--dynamic-only` и `--motion-threshold`
- `utils/__init__.py` - экспорт `DynamicObjectFilter`
- `utils/video_processor_advanced.py` - интеграция фильтра
- `utils/visualizer.py` - визуализация классификации

---

## Тестирование

### Быстрый тест
```bash
python main.py --source input/video.mp4 --dynamic-only --no-display
```

### Проверка классификации
```bash
python main.py --source input/video.mp4 --dynamic-only --classes person
```
Убедитесь, что на экране отображается:
- `MOTION: Walk` (или другой тип)
- `Speed: XX.Xpx/s`

### Проверка экспорта
```bash
python main.py --source input/video.mp4 --dynamic-only --export-data --no-display
```
Проверьте файл `output/run_*/analytics/detection_analysis_trajectories.json` на наличие поля `motion_classification`

---

## Дальнейшее развитие

### Планируемые улучшения
- [ ] Калибровка скорости в м/с (вместо px/s)
- [ ] Детекция аномального поведения
- [ ] Предсказание траектории движения
- [ ] Кластеризация паттернов движения
- [ ] Интеграция с Re-ID для долгосрочного трекинга

---

**Дата создания:** 23 ноября 2025  
**Версия:** 1.0  
**Автор:** AI Assistant (Claude Sonnet 4.5)

