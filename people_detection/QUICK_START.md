# 🚀 Быстрый старт

## Проверка экспорта данных (после исправления бага)

### 1. Простой тест
```bash
python main.py --source input/video.mp4 --export-data --classes person --no-display
```

### 2. Проверка результатов
Откройте папку `output/run_YYYYMMDD_HHMMSS/analytics/` - там должны быть файлы:
- ✅ `detection_analysis_trajectories.json`
- ✅ `detection_analysis_trajectories.csv`
- ✅ `detection_analysis_frames.json`
- ✅ `summary.txt`

### 3. Если файлы не создались
Смотрите подробную инструкцию в файле `BUGFIX_EXPORT.md`

---

## Основные команды

### Детекция с визуализацией
```bash
python main.py --source input/video.mp4 --classes person
```

### Адаптивный трекинг (умный)
```bash
python main.py --source input/video.mp4 --adaptive-tracking --show-trajectories
```

### Распознавание действий
```bash
python main.py --source input/video.mp4 --action-recognition --pose-detection
```

### Полный набор функций
```bash
python main.py --source input/video.mp4 \
  --adaptive-tracking \
  --action-recognition \
  --pose-detection \
  --show-heatmap \
  --show-stats-panel \
  --export-data
```

---

## Горячие клавиши (во время работы)

- `q` - выход
- `p` - показать/скрыть скелеты
- `a` - включить/выключить распознавание действий
- `h` - показать/скрыть heat map
- `t` - показать/скрыть траектории
- `s` - показать/скрыть панель статистики

---

## Документация

- **REFACTORING_SUMMARY.md** - полный отчет о рефакторинге
- **ROADMAP.md** - план развития проекта
- **BUGFIX_EXPORT.md** - исправление бага экспорта
- **TEST_EXPORT.md** - тестирование экспорта данных

---

## Структура проекта

```
people_detection/
├── main.py                 # Точка входа
├── utils/                  # Модули
│   ├── geometry.py         # Математика (НОВЫЙ)
│   ├── common.py           # Общие функции (НОВЫЙ)
│   ├── analytics.py        # Аналитика
│   ├── tracker.py          # Трекинг
│   ├── exporter.py         # Экспорт данных
│   └── ...
├── input/                  # Входные видео
├── output/                 # Результаты
│   └── run_YYYYMMDD_HHMMSS/
│       ├── analytics/      # JSON/CSV данные
│       ├── result.mp4      # Обработанное видео
│       └── floor_projection.mp4
└── requirements.txt
```

---

## Проблемы?

1. **Экспорт не работает** → `BUGFIX_EXPORT.md`
2. **Нужна помощь с тестами** → `TEST_EXPORT.md`
3. **Хочу узнать о планах** → `ROADMAP.md`
4. **Интересует детальный отчет** → `REFACTORING_SUMMARY.md`

