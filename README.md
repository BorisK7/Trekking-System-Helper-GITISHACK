# 🚀 YOLOv8 Advanced Object Detection & Tracking System

Расширенная система детектирования и трекинга объектов с использованием YOLOv8, оптимизированная для RTX 3060.

## ✨ Новые возможности

### 🎯 Трекинг и траектории
- **Отслеживание объектов** с уникальными ID
- **Визуализация траекторий** движения с градиентом и стрелками направления
- **Расчет скорости** движения объектов (пиксели/сек)
- **История перемещений** с временными метками
- **Статистика** по каждому треку (длительность, пройденное расстояние)

### 📊 Аналитика сцены
- **Heat Map** - тепловая карта активности в зонах
- **Зоны интереса** - определение и подсчет объектов в зонах
- **Линии подсчета** - вход/выход через заданные линии
- **Временная статистика** - количество объектов по времени
- **Занятость мест** - определение свободных/занятых стульев, мест

### 📏 Расчет расстояний
- **Расстояние от камеры** до объектов
- **Размеры объектов** в метрах (ширина, высота)
- **Расстояние между объектами**
- **Social distancing** - контроль дистанции между людьми

### 💾 Экспорт данных
- **JSON формат**: полные данные треков с метаданными
- **CSV формат**: координаты для анализа
- **UWB-совместимый формат**: для сравнения с UWB датасетом
- **Текстовые отчеты**: сводная статистика

## 📦 Установка

```bash
# Клонирование репозитория
cd people_detection

# Установка зависимостей
pip install -r requirements.txt

# Проверка CUDA
python -c "import torch; print(f'CUDA: {torch.cuda.is_available()}')"
```

### Зависимости
- ultralytics>=8.0.0
- opencv-python>=4.8.0
- numpy>=1.24.0
- torch>=2.0.0
- torchvision>=0.15.0
- scipy>=1.10.0

## 🎮 Использование

### Базовая детекция

```bash
# Все объекты на видео
python main.py --source input/video.mp4 --output output/result.mp4

# Только люди
python main.py --source input/video.mp4 --classes person

# Мебель и люди
python main.py --source input/video.mp4 --classes "person,chair,couch,bed,dining table"
```

### Трекинг и траектории

```bash
# С отображением траекторий (по умолчанию)
python main.py --source input/video.mp4 --output output/tracked.mp4 --show-trajectories

# С heat map активности
python main.py --source input/video.mp4 --show-heatmap

# С панелью статистики
python main.py --source input/video.mp4 --show-stats-panel

# Все функции вместе
python main.py --source input/video.mp4 --output output/full.mp4 
  --show-trajectories --show-heatmap --show-stats-panel
```

### Экспорт данных

```bash
# Экспорт траекторий для анализа
python main.py --source input/video.mp4 --export-data --classes person

# Указать директорию экспорта
python main.py --source input/video.mp4 --export-data 
  --export-dir output/my_analysis --classes person
```

Экспортируются файлы:
- `trajectories.json` - полные данные треков
- `trajectories.csv` - координаты точек
- `frame_data.json` - покадровые данные
- `statistics.json` - общая статистика
- `uwb_format.csv` - формат для сравнения с UWB
- `summary.txt` - текстовый отчет

### Без трекинга (только детекция)

```bash
python main.py --source input/video.mp4 --no-tracking
```

### Веб-камера

```bash
# С трекингом и траекториями
python main.py --source 0 --show-trajectories

# Без отображения, только сохранение
python main.py --source 0 --output output/webcam.mp4 --no-display
```

## ⌨️ Горячие клавиши

Во время обработки доступны:

- **Q** - Выход
- **P** - Пауза/Продолжение
- **H** - Вкл/Выкл Heat Map
- **T** - Вкл/Выкл траектории
- **S** - Вкл/Выкл панель статистики

## 🎛️ Параметры командной строки

### Основные

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `--source` | Путь к видео или 0 для веб-камеры | Обязательный |
| `--output` | Путь для сохранения результата | None |
| `--model` | Модель YOLOv8 (n/s/m/l/x) | yolov8m |
| `--conf` | Порог уверенности (0-1) | 0.5 |
| `--iou` | Порог IoU для NMS (0-1) | 0.45 |
| `--classes` | Классы для детекции | all |

### Трекинг и визуализация

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `--show-trajectories` | Показывать траектории | True |
| `--show-heatmap` | Показывать heat map | False |
| `--show-stats-panel` | Показывать панель статистики | False |
| `--no-tracking` | Отключить трекинг | False |

### Экспорт

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `--export-data` | Экспортировать данные | False |
| `--export-dir` | Директория экспорта | output/analytics |

### Прочее

| Параметр | Описание |
|----------|----------|
| `--no-display` | Не отображать результат |
| `--list-classes` | Показать все 80 классов COCO |

## 📊 Примеры использования

### Анализ движения людей

```bash
python main.py --source input/office.mp4 
  --output output/office_tracked.mp4 
  --classes person 
  --show-trajectories 
  --show-heatmap 
  --export-data
```

Результат:
- Видео с траекториями движения
- Heat map зон активности
- Экспорт всех траекторий в JSON/CSV

### Мониторинг занятости помещения

```bash
python main.py --source input/room.mp4 
  --classes "person,chair,couch,dining table" 
  --show-stats-panel 
  --export-data
```

Результат:
- Подсчет людей и мебели
- Статистика по времени
- Экспорт для анализа занятости

### Сравнение с UWB данными

```bash
python main.py --source input/uwb_test.mp4 
  --classes person 
  --export-data 
  --export-dir output/uwb_comparison
```

Результат файл `uwb_format.csv`:
```csv
track_id,x,y,z,timestamp,class
1,450.5,320.2,0.0,0.0,person
1,452.1,322.5,0.0,0.033,person
...
```

Можно сравнить с `walking_path.csv` из UWB датасета!

## 🎨 Визуализация

### Траектории
- **Цветные линии** для каждого трека
- **Градиент прозрачности** (старые точки бледнее)
- **Стрелки направления** движения
- **ID трека и скорость** над объектом

### Heat Map
- **Цветовая карта** от синего (низкая активность) до красного (высокая)
- **Накопительная карта** с затуханием
- **Полупрозрачное наложение** на видео

### Панель статистики
- Активные треки
- Распределение по классам
- Статистика зон
- Занятость мест

## 🔧 Производительность на RTX 3060

| Модель | FPS (1080p) | mAP | Рекомендация |
|--------|-------------|-----|--------------|
| yolov8n | ~140 | 37.3% | Реал-тайм, веб-камера |
| yolov8s | ~100 | 44.9% | Баланс |
| **yolov8m** | ~70 | 50.2% | **Рекомендуется** |
| yolov8l | ~45 | 52.9% | Высокая точность |
| yolov8x | ~30 | 53.9% | Максимальная точность |

*С трекингом FPS снижается на ~10-15%*

## 📖 Архитектура

```
people_detection/
├── main.py                          # CLI точка входа
├── utils/
│   ├── video_processor_advanced.py  # Основной процессор с трекингом
│   ├── tracker.py                   # Трекинг объектов (Hungarian algorithm)
│   ├── analytics.py                 # Heat map, зоны, занятость
│   ├── visualizer.py                # Отрисовка траекторий, зон
│   ├── distance.py                  # Расчет расстояний
│   ├── exporter.py                  # Экспорт данных
│   └── config.py                    # Конфигурация, 80 классов COCO
├── requirements.txt
└── README_ADVANCED.md
```

## 🔍 Технические детали

### Трекинг
- **Алгоритм**: Hungarian algorithm для ассоциации детекций
- **IoU Threshold**: 0.3 для сопоставления треков
- **Max Age**: 30 кадров без детекции до удаления трека
- **Min Hits**: 3 детекции для активации трека

### Аналитика
- **Heat Map**: Гауссово размытие с затуханием 0.95
- **Зоны**: Ray casting algorithm для проверки точки в полигоне
- **Скорость**: Экспоненциальное скользящее среднее (α=0.3)

### Экспорт
- **Траектории**: Список (x, y, timestamp) для каждого трека
- **Метаданные**: Длительность, расстояние, средняя скорость
- **UWB формат**: Совместим с `walking_path.csv`

## 🐛 Решение проблем

### Ошибка импорта scipy
```bash
pip install scipy>=1.10.0
```

### Низкий FPS
- Используйте `yolov8n` или `yolov8s`
- Отключите heat map: не используйте `--show-heatmap`
- Отключите панель статистики
- Уменьшите разрешение видео

### Пропущенные треки
- Увеличьте `max_age` в `tracker.py`
- Уменьшите `conf_threshold`: `--conf 0.3`
- Используйте более точную модель: `--model yolov8l`

## 📚 Интеграция с UWB

Проект совместим с вашим UWB датасетом:

1. **Экспорт траекторий YOLOv8**:
```bash
python main.py --source camera_feed.mp4 --export-data --classes person
```

2. **Сравнение с UWB** в Jupyter Notebook:
```python
import pandas as pd

# YOLOv8 траектории
yolo_df = pd.read_csv('output/analytics/uwb_format.csv')

# UWB траектории
uwb_df = pd.read_csv('data_set/environment0/walking_path.csv')

# Сравнение точности позиционирования
merged = pd.merge(yolo_df, uwb_df, on='timestamp', suffixes=('_yolo', '_uwb'))
error = np.sqrt((merged.x_yolo - merged.x_uwb)**2 + (merged.y_yolo - merged.y_uwb)**2)
print(f"Средняя ошибка позиционирования: {error.mean():.2f} пикселей")
```

## 🎯 Дальнейшее развитие

- [ ] Калибровка камеры по UWB якорям
- [ ] Гибридная локализация (YOLOv8 + UWB)
- [ ] Определение позы (сидит/стоит)
- [ ] Детекция падений
- [ ] Анализ взаимодействий объектов
- [ ] Grafana dashboard для статистики

## 📄 Лицензия

См. LICENSE в корне проекта

---

**Создано**: 2025  
**GPU**: NVIDIA RTX 3060 12GB  
**Framework**: YOLOv8 (Ultralytics)
